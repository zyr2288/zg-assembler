import { Instructoins } from "../Instructions";

export class AsmCommon {

	instruction = new Instructoins();

	protected AddInstruction(opCode: number, operation: string, addressType?: string) {

	}
}